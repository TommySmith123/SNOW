# 2D 单板滑雪网页游戏规格说明

## 1. 文档目的

本文档定义一款网页端 2D 单板滑雪游戏的首个可玩版本（MVP），供 Codex 直接据此实现。

第 1～22 节保留首个可玩版本的基线定义。第 23 节起定义 2026-07-28 的“商城与个性化”扩展版本；若两部分冲突，以扩展版本为准。本版本仍不包含账号、联网排行榜、多人模式或复杂物理模拟，但新增本地货币、商城、可换装备、宠物和背景音乐。

## 2. 产品概述

玩家控制一名单板滑雪者持续向山下滑行。画面采用 2D 俯视偏侧视角：玩家大致保持在屏幕中下部，雪道、地面纹理和障碍物由屏幕下方向上方滚动，从而表现持续前进。

角色始终沿左下或右下方向斜滑。玩家不能直接按左右键平移，只能通过一次“换刃”输入切换斜滑方向；同时可加速、减速和跳跃。目标是在难度不断提高的无限雪道上尽可能滑得更远。

核心循环：

> 观察前方雪道 → 判断路线与障碍 → 调整速度 → 选择换刃时机 → 必要时跳跃 → 继续滑行并刷新距离纪录

## 3. 设计原则

1. 操作简单：核心输入只有换刃、加速、减速、跳跃。
2. 换刃有惯性：方向不能瞬间反转，应先削弱原横向速度，再逐步建立反向速度。
3. 玩家可预判：屏幕下方必须提供足够的前方视野和反应时间。
4. 速度有取舍：高速距离增长快、跨越能力强，但反应时间短；低速更易控制，但得分慢且无法跨越宽裂缝。
5. 障碍无死局：任何生成组合都必须至少保留一条合理通过路线。
6. 跳跃不能万能：裂缝和低矮障碍可跳过，大树等高障碍必须绕行。
7. 首版重手感：优先保证换刃、速度和跳跃三者配合有趣，再扩展内容。

## 4. 目标平台与显示

### 4.1 平台

- 桌面浏览器优先。
- 支持现代 Chrome、Edge、Firefox 和 Safari。
- 首版使用键盘操作。
- 页面刷新后保留本地最高距离。

### 4.2 画布与适配

- 推荐竖向游戏区域，设计基准比例为 9:16。
- 游戏区域在窗口内等比缩放并居中。
- 宽屏两侧可使用背景延展或留白，不得拉伸游戏世界。
- 窗口尺寸改变后应保持可玩，不得使碰撞坐标和画面错位。
- 游戏运行中应阻止相关按键触发页面滚动。

### 4.3 世界与屏幕方向

- 屏幕上方：已经滑过的区域。
- 屏幕下方：即将到达的雪道。
- 雪道及其物体向上移动。
- 玩家纵向位置通常保持在屏幕高度约 60%～70% 处。
- 玩家横向位置随滑行方向和惯性变化。

## 5. 游戏状态

游戏至少包含以下状态：

1. `START`：开始界面。
2. `COUNTDOWN`：3、2、1、GO 倒计时。
3. `PLAYING`：正常游戏。
4. `PAUSED`：暂停。
5. `CRASHED`：碰撞或坠落后的短暂演出。
6. `GAME_OVER`：结算界面。

状态转换：

- 打开页面 → `START`
- 点击开始或确认键 → `COUNTDOWN`
- 倒计时结束 → `PLAYING`
- 暂停键 → `PAUSED`；再次按下 → `PLAYING`
- 致命碰撞 → `CRASHED`
- 摔倒演出结束 → `GAME_OVER`
- 重新开始 → `COUNTDOWN`

暂停或游戏结束时，世界更新、距离增长和碰撞检测必须停止。

## 6. 输入方案

默认键位：

| 操作 | 主按键 | 备用按键 | 输入方式 |
|---|---|---|---|
| 换刃 | Space | — | 单次按下 |
| 加速 | K | ArrowUp | 按住 |
| 减速 | L | ArrowDown | 按住 |
| 跳跃 | J | Shift | 单次按下 |
| 暂停/继续 | Esc | P | 单次按下 |
| 开始/重新开始 | Enter | — | 单次按下 |

输入要求：

- 按住换刃或跳跃不得因键盘自动重复而连续触发。
- 加速与减速同时按下时，减速优先。
- 空中按换刃键不改变当前空中横移方向，只预设落地后的刃向。
- `START` 和 `GAME_OVER` 界面应同时支持按钮点击。

## 7. 玩家运动模型

### 7.1 基本状态

玩家至少包含以下数据：

- 横向位置 `x`
- 屏幕基准纵向位置 `y`
- 当前前进速度
- 当前横向速度
- 当前刃向：左刃或右刃
- 当前动作状态
- 是否腾空
- 跳跃进度或高度
- 待落地刃向
- 是否处于短暂无敌或失控状态

### 7.2 自动滑行

- 游戏开始后角色自动向山下前进，不能完全停止。
- 前进速度决定雪道向上滚动速度和距离累积速度。
- 角色始终具有左下或右下的斜滑趋势。
- 首局初始刃向可固定为右刃，以保证行为可预期。

### 7.3 换刃

按下换刃键后：

1. 当前刃向切换为另一侧。
2. 当前横向速度不会瞬间反号。
3. 角色在约 0.15～0.30 秒内先减弱原横向速度，再建立新方向速度。
4. 角色播放重心转移和板身转平的过渡动画。
5. 高速时横移能力更强，但惯性也更明显。

实现时应将目标横向速度与实际横向速度分离，通过平滑逼近实现换刃。最终参数必须集中配置，便于调手感。

### 7.4 速度控制

速度为连续值，并受最小值与最大值限制：

- 无输入：速度缓慢回归巡航速度。
- 按住加速：速度平滑提升。
- 按住减速：速度平滑下降，但不低于最低滑行速度。
- 碰撞小障碍、落地或进入深雪时可瞬时或持续减速。

建议以三个区间辅助表现：

| 区间 | 手感与反馈 |
|---|---|
| 低速 | 易控制，距离增长慢，跳跃前进距离短 |
| 中速 | 默认巡航，风险和收益平衡 |
| 高速 | 横移和距离增长快，风声增强，反应时间缩短 |

界面显示实时速度数值或速度条；具体物理参数须放在统一配置对象中。

### 7.5 雪道边界

- 玩家不能无限离开雪道。
- 接触边缘时先明显减速并受到朝雪道内部的推力。
- 短时间擦边不直接结束。
- 持续越界或完全离开可判定摔倒。
- 边界必须有清晰视觉提示，如树林、护栏、旗帜或压雪边缘。

## 8. 跳跃系统

### 8.1 阶段

跳跃包含：

1. 起跳准备：短暂下蹲。
2. 上升。
3. 最高点。
4. 下落。
5. 落地缓冲。

完整过程建议约 0.6～1.0 秒。首版可使用确定性的跳跃曲线，不要求刚体物理。

### 8.2 速度影响

- 高速时水平跨越距离更长，可越过较宽裂缝。
- 低速时跨越距离较短。
- 跳跃高度可小幅随速度变化，但视觉和判定必须保持可理解。

### 8.3 空中操作

- 腾空后保留起跳瞬间的横向惯性。
- 空中加减速效果大幅降低或忽略。
- 空中换刃只修改待落地刃向。
- 不允许二段跳。
- 起跳、腾空和落地期间必须有明确阴影或高度反馈。

### 8.4 落地

- 安全落地：继续滑行，速度轻微下降，播放喷雪效果。
- 落点在实体障碍上：按障碍规则处理。
- 落点位于裂缝内：游戏结束。
- 落地后可有极短恢复时间，但不得让操作长期失灵。

## 9. 障碍与地形

MVP 必须实现以下三类：

| 类型 | 主要应对方式 | 未正确应对的结果 |
|---|---|---|
| 树木 | 换刃绕行 | 致命碰撞，游戏结束 |
| 石块 | 绕行；小石块可跳 | 擦碰大幅减速，大石块可致命 |
| 裂缝 | 提前跳跃 | 未腾空通过则坠落并结束 |

规则要求：

- 障碍轮廓必须在高速下仍易识别。
- 树木不可通过跳跃免疫。
- 裂缝判定应基于玩家与裂缝区域重叠时是否腾空。
- 石块尺寸应与其后果一致，避免视觉误导。
- 障碍碰撞体可略小于可见轮廓，以减少不公平擦碰。

后续版本可扩展倒木、冰面、深雪、雪包、旗门和其他滑雪者，但不属于 MVP 验收范围。

## 10. 无限雪道生成

### 10.1 区块机制

- 雪道由连续纵向区块组成。
- 上方区块完全离开视野后回收。
- 屏幕下方始终预生成足够长度的新区块，不能出现空白。
- 每个区块记录雪道中心、宽度、边界形状、障碍和视觉装饰。
- 雪道可缓慢左右弯曲，但相邻区块必须平滑衔接。

### 10.2 约束式随机

不得纯随机摆放障碍。生成器必须满足：

1. 每个障碍组至少保留一条可通过路线。
2. 裂缝前保留足够起跳距离。
3. 裂缝后提供安全落地区，不能紧接无法避开的障碍。
4. 不连续生成横跨整条雪道的障碍。
5. 障碍之间的横向和纵向间隔适应玩家最大换刃能力。
6. 按当前可能速度计算最小反应时间。
7. 开局教学距离内不生成复杂组合。

推荐使用“预制障碍模式 + 参数化变化”：

- 单棵树，左右均可绕行。
- 两个错位石块，形成 S 形路线。
- 窄裂缝，要求跳跃。
- 树与石块组合，保留明确通道。

生成前选择符合当前难度的模式，再在安全范围内改变位置、尺寸和间距。

### 10.3 可重复调试

- 随机生成器应支持可选种子。
- 开发模式可显示当前种子，便于复现死局或碰撞问题。
- 正式界面无需显示种子。

## 11. 距离、速度与纪录

### 11.1 距离

- 左上角始终显示当前距离，格式如 `1,248 m`。
- 距离按实际前进速度随时间累积。
- 暂停、倒计时、撞毁演出和结算时不得继续增加。
- 显示值取整，内部可保留小数。

### 11.2 速度

- 距离下方显示当前速度，如 `72 km/h`，并可配简洁速度条。
- UI 数值与实际游戏速度必须一致地映射。

### 11.3 最高纪录

- 游戏结束时比较本局距离和历史最高距离。
- 历史最高距离保存到浏览器本地存储。
- 刷新页面后仍可读取。
- 本地存储缺失或数据损坏时安全回退为 0。

MVP 以距离为唯一正式得分，不实现技巧分。

## 12. 难度曲线

| 距离 | 阶段 | 生成与速度特征 |
|---|---|---|
| 0～300 m | 教学 | 雪道宽、速度上限低、单个障碍、窄裂缝、间距大 |
| 300～1,000 m | 常规 | 密度上升、出现简单组合、雪道轻微弯曲 |
| 1,000～2,000 m | 困难 | 雪道变窄、组合更复杂、裂缝变宽、速度上限提高 |
| 2,000 m 以上 | 极限 | 高速为主、密度继续上升、复杂模式出现，但仍必须可解 |

难度参数应连续插值或分段平滑变化，不能在阈值处突然跃升。难度可影响：

- 障碍密度
- 组合模式权重
- 雪道宽度
- 雪道弯曲幅度
- 裂缝宽度
- 安全通道宽度
- 速度上限

## 13. 碰撞与失败

### 13.1 非致命结果

- 擦到小石块：明显减速，并短暂失控。
- 接触边界：减速并向内弹回。
- 普通落地：轻微减速。

非致命碰撞后可提供约 0.5～1.0 秒的短暂无敌，防止同一物体重复扣罚。

### 13.2 致命结果

- 正面撞树。
- 撞击大型岩石。
- 未腾空进入裂缝。
- 完全滑出雪道。

致命碰撞后：

1. 立即停止玩家输入和计分。
2. 播放短暂慢动作、震动或摔倒动画。
3. 进入结算界面。

碰撞判定必须按玩家当前状态执行：腾空时忽略可跳低矮障碍，但不能忽略树木等高障碍。

## 14. 界面与流程

### 14.1 开始界面

显示：

- 游戏名称。
- 最高距离。
- “开始游戏”按钮。
- 简明键位说明：Space 换刃、K/L 调速、J 跳跃。

### 14.2 倒计时

- 显示 `3`、`2`、`1`、`GO`。
- 倒计时期间世界和玩家可播放待机动画，但不计距离、不接受移动操作。

### 14.3 游戏 HUD

左上角：

- 当前距离，视觉优先级最高。
- 当前速度或速度条。

右上角可显示暂停按钮或暂停提示。HUD 不得遮挡主要前方视野。

### 14.4 游戏结束

显示：

- 本次距离。
- 历史最高距离。
- 若刷新纪录，显示明确提示。
- “重新开始”按钮。
- 返回开始界面的入口。

## 15. 视觉、动画与反馈

### 15.1 风格

首版采用清晰的简洁卡通风格：

- 雪地：白色或浅灰蓝。
- 冰雪阴影：浅蓝。
- 裂缝：深蓝至近黑。
- 树木和岩石：深色轮廓。
- 玩家：使用高饱和服装，与环境强对比。

地面纹理应帮助感知速度，但不能形成视觉噪声。

### 15.2 玩家动画

至少表现：

- 左刃滑行
- 右刃滑行
- 换刃过渡
- 加速前倾
- 减速横板喷雪
- 起跳、腾空、落地
- 摔倒

若首版没有序列帧资源，可用图形旋转、缩放、姿态变化、阴影和粒子表达，但状态必须清楚。

### 15.3 即时反馈

- 换刃：板身旋转、刮雪粒子、短促音效。
- 加速：地面纹理加快、风线增多、风声增强。
- 减速：喷雪增多、速度条回落。
- 跳跃：阴影与角色分离，角色略向屏幕上方抬升。
- 落地：喷雪、轻微镜头震动。
- 致命碰撞：更强震动、短暂停顿或慢动作。

镜头效果必须克制，不得影响障碍判断。

## 16. 音频

建议包含：

- 持续滑雪摩擦声，音高或音量随速度变化。
- 换刃刮雪声。
- 加速风声。
- 减速喷雪声。
- 起跳和落地声。
- 石块、树木碰撞声。
- 坠入裂缝声。
- 新纪录提示声。

浏览器音频应在用户首次交互后启动，避免自动播放限制。游戏失去焦点或暂停时，应暂停或降低循环音频。

## 17. 技术与架构要求

实现技术可由 Codex 根据当前项目环境选择，但需满足：

- 游戏循环以时间差更新，不能将移动速度绑定到帧数。
- 渲染与游戏状态更新逻辑分离。
- 所有关键手感参数集中配置，禁止散落魔法数字。
- 按职责拆分为输入、玩家、世界/雪道、生成、碰撞、UI、音频和存储模块。
- 对象离开视野后必须回收或销毁，避免无限增长。
- 页面切换后台、窗口失焦或时间差异常增大时自动暂停或限制单帧最大时间差。
- 碰撞、距离和世界坐标使用同一套明确的坐标约定。

建议的逻辑模块：

1. `GameState`：状态机、主循环、重开。
2. `InputManager`：键盘与按钮输入。
3. `PlayerController`：速度、换刃、跳跃和状态。
4. `TrackManager`：区块生成、滚动、回收和边界。
5. `ObstacleManager`：障碍模式、生成与回收。
6. `CollisionSystem`：玩家、障碍、裂缝和边界判定。
7. `DifficultyManager`：按距离输出难度参数。
8. `HUD`：距离、速度、暂停与结算。
9. `AudioManager`：循环音频和事件音效。
10. `StorageService`：本地最高纪录。

## 18. 推荐初始可调参数

以下值仅用于建立第一版手感，必须易于调整：

| 参数 | 推荐初始范围 |
|---|---|
| 初始速度 | 中低速 |
| 最低速度 | 初始速度的约 50%～70% |
| 最高速度 | 初始速度的约 180%～250% |
| 换刃过渡时间 | 0.15～0.30 秒 |
| 跳跃总时长 | 0.6～1.0 秒 |
| 非致命碰撞无敌 | 0.5～1.0 秒 |
| 玩家屏幕纵向位置 | 屏幕高度的 60%～70% |
| 教学安全距离 | 前 100～300 m |

实际数值以可玩性测试为准，不应把这些范围视为硬性物理事实。

## 19. MVP 范围

### 19.1 必须实现

- 无限向上滚动的雪道。
- 玩家自动斜向滑行。
- 单键切换左右刃，并有平滑惯性。
- 加速与减速。
- 单次跳跃与安全落地。
- 树木、石块和裂缝。
- 约束式随机生成，保证基本可通过。
- 雪道边界与碰撞。
- 左上角距离和速度。
- 开始、倒计时、暂停、摔倒、结算、重开。
- 随距离增长的难度。
- 浏览器本地最高纪录。
- 基础视觉和音效反馈；无音效资源时至少保留接口。

### 19.2 明确不做

- 登录、云存档和联网排行榜。
- 多地图、天气和昼夜。
- 联机或 AI 对手。
- 花式动作计分。
- 复杂刚体、布料或真实雪地模拟。
- 完整移动端触控适配。

## 20. 验收标准

满足以下条件可认为 MVP 完成：

1. 页面打开后可从开始界面进入游戏，并可正常重开。
2. 玩家无需按前进键即可持续滑行。
3. 每次有效换刃输入只切换一次方向，且方向变化有可见惯性。
4. 加速和减速均为平滑变化，且速度存在上下限。
5. 跳跃不能二段跳；腾空可越过裂缝和低矮障碍。
6. 撞树、撞大石或掉入裂缝会结束游戏。
7. 小石块擦碰和雪道边缘按非致命规则处理。
8. 雪道可持续生成并回收，无明显接缝、空白或内存持续增长。
9. 前 300 米难度明显低于 1,000 米以后。
10. 障碍模式在正常玩法下不存在明显必死组合。
11. 左上角距离持续正确增长，高速增长快于低速。
12. 暂停后运动、碰撞和计分全部停止。
13. 最高距离在刷新页面后仍保留。
14. 常见桌面窗口尺寸下画面不拉伸，输入、渲染和碰撞保持一致。
15. 游戏失去焦点时不会因巨大时间差导致角色瞬移或直接死亡。

## 21. 实现顺序建议

1. 建立画布、状态机和固定玩家基准位置。
2. 实现雪道滚动、距离和速度模型。
3. 实现左右刃、横向惯性和边界。
4. 实现跳跃、阴影和落地。
5. 实现单个树、石块、裂缝及碰撞。
6. 实现区块生成、回收和障碍模式。
7. 接入难度曲线。
8. 完成开始、暂停、结算和本地纪录。
9. 补充动画、粒子、镜头和音频反馈。
10. 使用固定随机种子测试可通过性、碰撞公平性和长时间稳定性。

## 22. 开发完成前重点测试

- 连续快速按换刃键时方向状态是否正确。
- 起跳瞬间、最高点和落地瞬间的裂缝判定是否公平。
- 空中换刃是否只影响落地后的方向。
- 高速下是否仍有足够视野和反应时间。
- 障碍回收后是否仍参与碰撞。
- 重开后是否残留旧障碍、输入、音频或定时器。
- 暂停和切换浏览器标签页后是否出现距离跳变。
- 本地纪录数据异常时是否能正常启动。
- 长时间运行后区块和障碍数量是否保持稳定。
- 不同窗口尺寸和设备像素比下视觉与碰撞是否一致。

---

本文档是首版实现基线。如设计细节发生冲突，应优先遵循：可预判、无死局、换刃有惯性、速度有取舍、核心输入简单。

---

## 23. 商城与个性化扩展版本

### 23.1 目标

扩展版本在不改变原始主角身份和核心滑雪操作的前提下，加入“游玩—结算—获得薯薯币—购买与搭配—再次游玩”的长期循环。

设计原则：

1. 初次打开游戏时，主角保持当前默认造型：红发、黑色帽子、深灰雪镜、黑色衣裤和红色雪板。
2. 所有装备只改变指定部位；红发、脸部、白色耳翼、角色比例和动作轮廓保持不变。
3. 只有雪板影响性能，服装、雪镜、帽子和宠物不提供数值加成，避免强制审美选择。
4. 商城、货币、已购内容和当前搭配均使用浏览器本地存档。
5. 不使用真实品牌、付费、登录或网络交易。

### 23.2 开始界面入口

开始界面新增：

- 当前薯薯币余额。
- “薯薯商城”按钮。
- 背景音乐开关。

打开商城时游戏世界保持在 `START` 或 `GAME_OVER`，不更新距离和碰撞。商城使用覆盖式面板，支持滚动、分类筛选和关闭，不离开当前页面。

## 24. 薯薯币与结算

### 24.1 获得方式

每局从 `CRASHED` 首次进入 `GAME_OVER` 时结算一次：

`本局薯薯币 = 15 + floor(本局距离 / 10)`

示例：

| 距离 | 获得薯薯币 |
|---:|---:|
| 100 m | 25 |
| 500 m | 65 |
| 1,000 m | 115 |
| 2,500 m | 265 |

规则：

- 同一局不得因重复渲染或返回界面重复领取。
- 结算界面明确显示本局获得数量和当前余额。
- 0～99 米仍有 15～24 枚基础奖励，避免新玩家完全没有进度。
- 货币只在本地使用，不具有现实价值。

### 24.2 定价节奏

目标是让 500～1,000 米水平的玩家每 2～5 局可获得一个普通外观；高性能雪板和宠物需要更长目标，但不超过约 8 局常规游玩。

## 25. 商品目录与定价

### 25.1 雪板

| ID | 名称 | 价格 | 最高速度 | 加速度 | 定位 |
|---|---|---:|---:|---:|---|
| `board-classic` | 经典红板 | 0 | 152 km/h | 18 | 默认，均衡 |
| `board-sprout` | 雪芽板 | 240 | 158 km/h | 20 | 更容易提速 |
| `board-ice` | 冰箭板 | 520 | 170 km/h | 17 | 高极速、慢起步 |
| `board-comet` | 薯星彗尾 | 900 | 182 km/h | 22 | 高级全能板 |

雪板性能只替换全局运动模型中的 `maxSpeed` 和 `acceleration`。碰撞、刹车、跳跃和障碍生成规则保持一致。速度 HUD 必须按当前雪板上限计算。

### 25.2 裤子

| ID | 名称 | 价格 | 颜色 |
|---|---|---:|---|
| `pants-black` | 夜行雪裤 | 0 | 黑色 |
| `pants-navy` | 深海雪裤 | 100 | 深蓝 |
| `pants-scarlet` | 枫红雪裤 | 180 | 暗红 |
| `pants-white` | 雪原雪裤 | 240 | 雪白灰边 |

### 25.3 衣服

| ID | 名称 | 价格 | 颜色 |
|---|---|---:|---|
| `jacket-black` | 原点连帽衫 | 0 | 黑色 |
| `jacket-blue` | 冰湖外套 | 140 | 冰蓝 |
| `jacket-berry` | 山莓外套 | 220 | 深红 |
| `jacket-lime` | 极光外套 | 320 | 荧光黄绿 |

### 25.4 雪镜

| ID | 名称 | 价格 | 镜片 |
|---|---|---:|---|
| `goggles-graphite` | 星点石墨镜 | 0 | 深灰蓝点 |
| `goggles-amber` | 琥珀雪镜 | 120 | 金橙 |
| `goggles-aurora` | 极光雪镜 | 240 | 蓝紫渐变 |
| `goggles-rose` | 玫瑰雪镜 | 320 | 粉红 |

### 25.5 帽子

| ID | 名称 | 价格 | 颜色 |
|---|---|---:|---|
| `hat-black` | 原点黑帽 | 0 | 黑色 |
| `hat-cream` | 奶油针织帽 | 100 | 米白 |
| `hat-red` | 山莓红帽 | 180 | 红色 |
| `hat-sky` | 晴空蓝帽 | 260 | 浅蓝 |

### 25.6 宠物

| ID | 名称 | 价格 | 形象 |
|---|---|---:|---|
| `pet-digger` | 挖挖机 | 360 | 典型橘色仓鼠、奶油色腹部 |
| `pet-car` | 车车 | 520 | 典型金渐层猫、金色脸胸、深色毛尖、绿色眼睛 |

宠物初始未拥有、未启用。购买后可独立切换“跟随/休息”；允许两只宠物同时跟随。

## 26. 购买、装备与本地存档

### 26.1 商城行为

每件商品显示名称、价格、所属分类和简短说明。按钮状态：

- 未拥有且余额足够：`购买`
- 未拥有且余额不足：`还差 N 枚`
- 已拥有但未装备：`使用`
- 已装备：`使用中`
- 宠物已拥有但未跟随：`跟随`
- 宠物正在跟随：`休息`

购买成功后立即扣除薯薯币并自动装备；宠物购买后自动开始跟随。余额不得为负数。

### 26.2 存档结构

使用单一本地键 `shushu-profile-v2`，数据至少包含：

- `coins`
- `ownedItemIds`
- `equipped.board`
- `equipped.pants`
- `equipped.jacket`
- `equipped.goggles`
- `equipped.hat`
- `equippedPets`
- `musicEnabled`
- `testMode`

默认商品必须始终视为已拥有。读取失败、字段缺失或旧版本数据异常时，应逐字段回退，不能阻止游戏启动。最高纪录继续兼容原 `snowline-best` 键。

## 27. 隐藏测试状态

商城面板右下角放置一个低可见度的雪花按钮，正常浏览时不抢夺注意力，键盘聚焦时必须可见，并保留可访问名称“启用商城测试模式”。

点击后：

1. `testMode` 切换为开启。
2. 薯薯币至少提升至 99,999。
3. 全目录商品加入已拥有列表。
4. 所有装备与宠物可立即切换使用。
5. 商城显示“测试模式”标记，避免与正常进度混淆。

再次点击可关闭测试标记，但不回收已经解锁的商品和余额，防止破坏当前搭配。

## 28. 可换装备的游戏内表现

Canvas 主角绘制函数接收当前搭配：

- 雪板：改变板身主色、装饰线和性能参数。
- 裤子：改变双腿/雪裤颜色，靴子保持深色。
- 衣服：改变连帽衫主体颜色，保留浅蓝色心形鹿角图案。
- 雪镜：改变镜片颜色和高光，保持帽顶大雪镜轮廓。
- 帽子：改变针织帽主体颜色。

装备切换只允许在商城中进行。进入新一局时读取当前搭配；游戏中途不弹出商城。

## 29. 宠物视角与跟随算法

### 29.1 视觉角度

挖挖机和车车使用俯视偏侧、面朝斜下方的三分之四视角，不再正脸朝向屏幕：

- 头部位于身体前下方并略向一侧偏移。
- 只突出近侧眼睛和近侧前爪，远侧五官弱化。
- 尾巴从身体后上方延伸。
- 身体长轴与路线切线保持同向，但旋转幅度限制在舒适范围，避免快速翻转。

车车必须改为金渐层：暖金底色、奶油色胸口、棕黑色毛尖与条纹、绿色眼睛；游戏和分享封面保持一致。

### 29.2 跟随与防回退

宠物使用“只按历史距离采样”的路线跟随：

1. 每只宠物使用固定纵向滞后距离。
2. 目标纵向位置只由 `model.distance - lagMeters` 决定。
3. 横向偏移只影响屏幕 X，不再把路线法线的 Y 分量加入宠物位置。
4. 宠物屏幕 Y 固定为与主人距离对应的单调位置，频繁换刃时不得向屏幕下方回退。
5. 切线角度使用相邻多个采样点平均，并限制最大显示旋转角。
6. 起步时历史轨迹不足，使用从主人当前位置向后延伸的稳定虚拟路线；不跳到更早轨迹。

雪痕继续使用等距采样。宠物爪印按各自路线离散绘制，不绘制连续“雪板线”。

## 30. 背景音乐

### 30.1 风格

背景音乐采用原创程序化 Web Audio 循环，不加载外部受版权保护的歌曲：

- 明快但不喧闹的冬季电子/合成器风格。
- 约 118 BPM。
- 轻打击节奏、短促低音和清亮琶音。
- 循环长度 8 小节，避免明显断点。
- 音量低于换刃、跳跃和碰撞提示音。

### 30.2 播放规则

- 默认开启，但必须等待用户首次点击“开始滑行”或音乐按钮后启动。
- 开始界面和正常游戏可播放。
- `PAUSED`、页面失焦或隐藏时暂停/显著降低音量。
- `GAME_OVER` 可保留低音量或停止。
- 开始界面提供明确的“音乐 开/关”按钮，选择写入本地存档。
- 音乐调度器和事件音效共用一个 `AudioContext`，卸载组件时停止定时器与音频节点。

## 31. 扩展版本验收标准

1. 开始界面能进入薯薯商城并显示余额。
2. 每局只结算一次薯薯币，所得与距离公式一致，刷新后保留。
3. 商品余额不足时不能购买，购买后正确扣款并持久保存。
4. 五类装备都能购买、装备，并在下一局 Canvas 角色上明显体现。
5. 四块雪板分别使用对应最高速度和加速度，HUD 上限同步变化。
6. 隐藏雪花按钮能开启测试状态、解锁全商品并提供足够余额。
7. 挖挖机和车车必须购买后才出现，且可分别设置跟随或休息。
8. 车车在游戏和分享封面中均为金渐层，不残留蓝灰主色。
9. 宠物呈斜向下三分之四视角，连续快速换刃时屏幕 Y 不回退。
10. 背景音乐在用户交互后播放，可关闭，暂停和页面隐藏时正确处理。
11. 原有最高纪录、键位、冰缝公平判定和默认主角造型不回退。

## 32. 宠物海报一致性改版

### 32.1 视觉基准

商城预览和游戏内宠物以当前宣传封面 `web/public/og.png` 为唯一造型基准，不再使用系统 Emoji。由于游戏内宠物尺寸较小，不直接缩放海报，而是提炼以下高识别度特征，用 Canvas 和 CSS 原生图形重绘：

- 统一使用粗暖棕描边、圆润身体、柔和的金橙渐变和明显的眼睛高光。
- 挖挖机：橙金色蓬松圆身、奶油白脸颊和腹部、圆耳、黑色大眼、粉色小鼻、两只向前伸的小爪，整体像海报中向下坡奔跑的小仓鼠。
- 车车：圆润短腿金渐层、奶油白口鼻和胸口、绿色大眼、额头与四肢有暖棕虎斑、粗而上翘的蓬松尾巴，整体像海报中的金渐层幼猫。
- 商城卡片中的宠物预览采用与游戏内相同的配色、轮廓和面部特征，不再显示 🐹 / 🐈 字符。
- 游戏仍保持斜向下三分之四视角，但头部可以略朝向玩家，使眼睛和表情可辨识。

### 32.2 小尺寸可读性

- 车车游戏内占地约 50 × 48 像素，挖挖机约 42 × 40 像素。
- 眼睛高光、奶油色口鼻、前爪和尾巴必须在 1× 显示尺寸下可见。
- 毛发质感只使用少量短弧线与色块表示，避免高频细线造成闪烁。
- 跳动只改变整体 Y 偏移，不改变身体各部件比例，避免跑动时“散架”。

## 33. 雪板性能与花纹重平衡

商城雪板从 4 块扩充为 8 块。旧雪板保留 ID 与购买状态，但重做数值，使升级差异在实际游玩中明显。极速决定速度上限，加速决定按住 K 时每秒增加的速度。

| ID | 名称 | 价格 | 极速 | 加速 | 花纹定位 |
|---|---|---:|---:|---:|---|
| `board-classic` | 经典红板 | 0 | 150 | 16 | 红色板面、奶油色中线 |
| `board-sprout` | 雪芽板 | 260 | 170 | 23 | 青柠嫩芽与叶片 |
| `board-ice` | 冰箭板 | 560 | 190 | 19 | 冰蓝箭羽和折线 |
| `board-comet` | 薯星彗尾 | 980 | 215 | 27 | 紫色星点和金色彗尾 |
| `board-tiger` | 雪岭虎纹 | 1,450 | 235 | 22 | 橙黑斜向虎纹 |
| `board-aurora` | 极光流光 | 2,100 | 255 | 31 | 深蓝底青紫极光波纹 |
| `board-dragon` | 赤焰龙脊 | 3,000 | 280 | 25 | 深红底金色龙鳞与火焰 |
| `board-hyper` | 薯薯超新星 | 4,200 | 305 | 36 | 黑紫宇宙底、霓虹星芒和薯星徽记 |

性能梯度设计：

- 入门板以低价快速解锁，主要提升加速手感。
- 中阶板分为“更高极速但加速温和”和“全能型”两条路线。
- 高阶板的极速和加速分别形成明显台阶，最高级板两项都显著领先，供测试模式和长期积累使用。
- HUD 速度上限、加速逻辑与商城标注必须来自同一商品数据，避免显示和实际不一致。
- 每个花纹同时用于商城缩略图和游戏板面；游戏板面至少包含两种装饰色，不能只换纯色。

## 34. 背景音乐响度改版

- 程序音乐主总线由约 `0.055` 提高到 `0.24`，主观响度约提升四倍。
- 主总线前加入动态压缩器，限制多音符叠加时的峰值，防止爆音。
- 旋律、低音和节奏之间保持原有比例，事件提示音继续清晰可辨。
- 淡入时间保持约 0.3 秒，关闭音乐时快速淡出。
- 不改变浏览器“必须先有一次用户交互才能播放”的规则。

## 35. 本轮验收标准

1. 商城中不再使用 Emoji 展示宠物，挖挖机和车车的预览与宣传封面具有一致的圆润轮廓、配色和面部特征。
2. 游戏内挖挖机具有橙金圆身、奶油脸颊、黑色亮眼与前爪；车车具有金渐层、绿色亮眼、虎斑、奶油口鼻和蓬松上翘尾巴。
3. 宠物继续维持斜向下视角和稳定跟随，不回退之前的轨迹修复。
4. 商城共有 8 块雪板，花纹在商城和游戏内均可区分。
5. 8 块雪板的极速和加速严格使用第 33 节数值，旧存档仍能正常读取。
6. 音乐比上一版明显更响，连续播放不削波、不爆音，音乐开关和暂停行为保持正常。

## 36. 跳跃期间的树木与岩石判定

树木和岩石只检测雪板在地面的接触碰撞：

- 当 `isAirborne(model) === true` 时，树干、小石头和大石头一律不产生碰撞，不考虑角色头部、身体或雪板在屏幕上的图形重叠。
- 落地后的第一帧恢复正常碰撞；是否落地只使用 `jumpTime` / `isAirborne` 判断，不能使用绘图高度的近似阈值。
- 树木仍只以树干底部为碰撞区域；树冠永远只是视觉层。
- 小石头在地面碰撞时继续减速并弹开，大石头在地面碰撞时为致命碰撞。
- 冰缝继续使用独立的雪板离地间隙规则，不受本节“空中忽略树石”影响。

## 37. 服装、裤子与帽子扩充

每个分类从 4 件扩充到 8 件。旧商品 ID、价格和拥有状态保持兼容；新增商品同时改变商城缩略图和游戏内角色造型。

### 37.1 新增裤子

| ID | 名称 | 价格 | 主色 / 装饰色 | 款式与图案 |
|---|---|---:|---|---|
| `pants-checker` | 雪线棋盘裤 | 380 | 深蓝 / 青柠 | 双腿棋盘格护膝 |
| `pants-cargo` | 山地机能裤 | 520 | 橄榄绿 / 奶油白 | 宽松工装与侧袋 |
| `pants-flame` | 赤焰雪裤 | 720 | 黑色 / 橙红 | 裤脚火焰纹 |
| `pants-aurora` | 极光流线裤 | 900 | 深紫 / 青蓝 | 双腿渐变流线 |

### 37.2 新增衣服

| ID | 名称 | 价格 | 主色 / 装饰色 | 款式与图案 |
|---|---|---:|---|---|
| `jacket-puffer` | 薯黄羽绒服 | 420 | 芥末黄 / 奶油白 | 横向绗缝羽绒服 |
| `jacket-checker` | 冰湖棋盘衣 | 600 | 冰蓝 / 深海蓝 | 大块棋盘格 |
| `jacket-flame` | 赤焰冲锋衣 | 840 | 黑色 / 红橙 | 下摆火焰纹 |
| `jacket-star` | 薯星夜行衣 | 1,100 | 深紫 / 荧光黄 | 胸前薯星与散落星点 |

### 37.3 新增帽子

| ID | 名称 | 价格 | 主色 / 装饰色 | 款式 |
|---|---|---:|---|---|
| `hat-pom` | 奶油绒球帽 | 340 | 奶油白 / 莓红 | 顶部绒球与翻边 |
| `hat-cat` | 金渐层猫耳帽 | 480 | 金色 / 暖棕 | 两只猫耳 |
| `hat-trapper` | 雪原护耳帽 | 650 | 橄榄绿 / 奶油白 | 两侧护耳与毛绒边 |
| `hat-helmet` | 薯星竞速盔 | 880 | 深紫 / 青柠 | 流线硬壳与中央星纹 |

绘制要求：

- 新裤子的护膝、侧袋、火焰或流线至少有一项在角色正常尺寸下可辨识。
- 新衣服不能只更换纯色；绗缝、棋盘、火焰或薯星图案必须显示。
- 新帽子改变外轮廓，不只是变色。
- 商城缩略图必须使用与游戏内一致的主色、装饰色和图案语义。

## 38. 宠物携带收益

### 38.1 挖挖机：薯薯币加成

当 `equippedPets` 包含 `pet-digger` 时，本局结算薯薯币获得 25% 加成：

```text
baseReward = 15 + floor(distance / 10)
diggerBonus = floor(baseReward × 0.25)
totalReward = baseReward + diggerBonus
```

- 加成只在游戏结束结算一次，与原有防重复结算逻辑共用。
- 结算界面显示“挖挖机收益 +25%”，并显示最终总收入。
- 测试模式不改变加成比例。

### 38.2 车车：一次性透明护盾

当开始一局时 `equippedPets` 包含 `pet-car`，角色获得 1 次护盾：

- 护盾以围绕角色的淡青色椭圆薄膜表示，常态透明度约 0.12，不能遮挡角色和障碍。
- 护盾只在即将发生致命碰撞时消耗，包括地面撞树、大石头或未成功越过冰缝。
- 小石头的普通减速碰撞不消耗护盾。
- 消耗时取消本次死亡、标记当前障碍已处理，角色获得短暂无敌并降低部分速度；护盾闪亮后消失。
- 每局最多抵挡一次，重新开始新一局时恢复；中途暂停、刷新 HUD 或换刃不能恢复。
- 未携带车车时护盾次数为 0。
- HUD 在护盾可用时显示一个低对比度“车车护盾”状态，消耗后变为“护盾已用”并短暂提示。

两只宠物可以同时携带，因此薯薯币加成和一次性护盾可以同时生效。

## 39. 本轮验收标准

1. 角色处于空中时穿过树木或任何大小岩石不会减速、弹开或死亡。
2. 角色落地后树干、小石头和大石头恢复原有地面判定，冰缝判定不回退。
3. 裤子、衣服和帽子各新增 4 件，共各 8 件；旧存档正常读取。
4. 新增服装在商城和游戏内都能辨认出对应图案或轮廓款式。
5. 携带挖挖机时结算收入严格按照基础收入加 25% 向下取整计算，且每局只结算一次。
6. 携带车车开始游戏时获得一次透明护盾，第一次致命碰撞被抵消，第二次正常结束游戏。
7. 两只宠物同时携带时，两种收益互不冲突。

## 40. Web、PWA 与 Mobile 的目录边界

项目采用“一套游戏逻辑、两个独立交付入口”：

```text
D:\AI\snow-repo\
├─ web\                         # 网页版与 PWA，继续由 Sites 部署
│  ├─ app\game\                 # 游戏逻辑与 Canvas 组件的唯一源码
│  ├─ public\                   # 网页/PWA 图标、manifest、service worker
│  └─ .openai\hosting.json      # 仅网页版部署使用
├─ mobile\                      # 手游封装，禁止放入 web\
│  ├─ src\                      # 手机壳入口与原生能力桥接
│  ├─ android\                  # Android Studio 原生工程
│  ├─ ios\                      # Xcode 原生工程
│  ├─ www\                      # 由移动端构建生成，不提交
│  ├─ capacitor.config.ts
│  ├─ vite.config.ts
│  └─ package.json
└─ spec.md                      # 随仓库保存的规格副本
```

边界规则：

- `web/` 不得导入 `mobile/` 中的任何文件，保证网页版构建和 Sites 部署不依赖手机工具链。
- `mobile/` 的 Vite 入口可以复用 `web/app/game/` 中的游戏组件与数据，但只输出到 `mobile/www/`。
- `mobile/www/`、Gradle 构建产物、Xcode DerivedData 和签名文件必须忽略，不进入网页版部署。
- Capacitor 使用本地 `www` 资源，不使用 `server.url` 指向线上地址；手游断网后仍能开始游戏。
- 网页版网址、Sites `project_id` 和 `.openai/hosting.json` 保持不变。

## 41. 手机触控输入

键盘操作继续完整保留，同时在触屏或窄屏设备显示四个触控区：

- 左侧大按钮“换刃”：单次点击等价于 `SPACE`。
- 右侧大按钮“跳跃”：单次点击等价于 `J`。
- “加速”按钮：按住期间等价于按住 `K`，抬手、取消触摸、窗口失焦时立即释放。
- “减速”按钮：按住期间等价于按住 `L`，抬手、取消触摸、窗口失焦时立即释放。

触控要求：

- 使用 Pointer Events，同一套代码兼容触摸、触控笔与鼠标测试。
- 所有按钮设置 `touch-action: none`，防止双击缩放、页面滚动和长按菜单。
- `pointerup`、`pointercancel`、`pointerleave` 和页面失焦都必须清空持续输入，避免“卡住加速”。
- 控件只在 `PLAYING` / `PAUSED` 的游戏舞台中显示；暂停时按钮降到不可操作状态。
- 按钮避开 iPhone/Android 的 `safe-area-inset-bottom`，且不能遮挡主角、速度 HUD 或暂停按钮。
- 手机开始界面的操作说明显示触控文案；桌面仍显示 `SPACE / K / L / J`。
- 支持轻量震动事件：换刃、跳跃、护盾破裂和撞击向移动端原生桥发送事件；普通网页在支持时使用 `navigator.vibrate`，不支持时静默跳过。

## 42. 手机布局与生命周期

- 继续使用 540 × 960 的竖屏逻辑画布，CSS 等比缩放，不改变碰撞坐标系。
- 小屏幕游戏舞台占满可用视口，并使用 `100dvh` 与四边安全区。
- PWA/手游模式隐藏桌面品牌侧栏，只保留游戏舞台。
- 禁止页面滚动、文本选择和橡皮筋误操作；商城内部仍允许纵向滚动。
- 页面隐藏、应用切后台、原生生命周期进入非活动状态时自动暂停并释放持续输入。
- 回到前台时不自动恢复滑行，由玩家点击继续，防止无准备碰撞。

## 43. PWA 安装与离线

网页版增加：

- `/manifest.webmanifest`：名称“薯薯雪线”、简称“薯薯雪线”、独立窗口、竖屏、主题色与背景色。
- 192 × 192、512 × 512 和 maskable 应用图标。
- `/sw.js` Service Worker：缓存游戏壳、图标、manifest 和访问过的静态构建资源。
- 首次在线成功加载后，后续离线可打开开始界面、商城并开始游戏。
- HTML metadata 包含 manifest、Apple Web App、主题色与应用图标。
- 浏览器触发 `beforeinstallprompt` 时，开始界面显示“安装到手机”按钮；不支持安装提示的浏览器不显示空按钮。
- Service Worker 更新采用版本化缓存；激活新版本后清理旧缓存，不能永久卡在旧版。

离线存档继续使用 `localStorage`。PWA、普通浏览器和原生 App 的存储空间彼此独立，本阶段不实现账号或云同步。

## 44. Capacitor 手游封装

`mobile/` 使用 Capacitor 8 稳定版和独立 Vite 静态构建：

- 应用 ID：`com.shushu.snowline`
- 应用名：`薯薯雪线`
- Web 资源目录：`www`
- 默认方向：竖屏
- 背景色：`#071b2b`
- Android 工程位于 `mobile/android/`
- iOS 工程位于 `mobile/ios/`

移动端入口直接渲染共享的 `SnowGame`，不嵌套 iframe、不访问生产网址。原生桥负责：

- Haptics：响应游戏发出的轻触、中触和重触事件。
- App lifecycle：切后台时向游戏发送暂停/释放输入事件。
- 状态栏/安全区：保持深色背景并避免内容进入不可见区域。

移动端脚本：

- `build`：构建本地静态游戏到 `mobile/www/`
- `sync`：构建后执行 `cap sync`
- `android`：同步并打开 Android Studio
- `ios`：同步并打开 Xcode（只能在 macOS/Xcode 环境完成）

## 45. 手游/PWA 验收标准

1. 桌面网页版键盘操作、公开网址、商城、存档、音乐与所有既有玩法不回退。
2. 手机浏览器可用触控完成开始、换刃、按住加速、按住减速、跳跃、暂停和商城操作。
3. 任意触控取消或切后台后，加速/减速状态不会残留。
4. PWA manifest、192/512 图标和 Service Worker 均可通过构建产物访问。
5. 首次在线加载后断网刷新，仍能打开游戏并读取本地装备与纪录。
6. `web/` 单独构建时不需要安装 `mobile/` 依赖。
7. `mobile/` 静态构建不包含线上 `server.url`，生成内容可由 Capacitor 本地加载。
8. Android 与 iOS 原生工程分别保存在 `mobile/android/`、`mobile/ios/`，不进入 Sites 发布包。
9. 网页与移动端都使用同一份游戏组件源码，不复制一套容易分叉的玩法代码。

## 46. 原生手游触控按键分区

本节只覆盖 Capacitor 原生手游壳（`body.native-mobile`）的游戏内触控布局。普通网页版和可安装 PWA 继续使用第 41 节已有布局，不随本节改变。

### 46.1 左右分区

- 左侧只放一个“换刃”按钮，不放加速、减速或跳跃。
- 右侧形成一个独立操作组，包含“加速”“减速”和“跳跃”三个按钮。
- 加速与减速并排放在右侧操作组上方；跳跃按钮位于同一右侧操作组下方并靠右。
- 左右操作组之间保留明确的空白区域。中间区域不得放置任何按钮，也不得加入会截获触摸的透明控件。
- 四个操作的事件语义、持续按压和释放规则保持第 41 节定义不变。

### 46.2 尺寸与可触达性

- “换刃”和“跳跃”使用不小于约 `5.4rem` 的圆形触控区。
- “加速”和“减速”各自使用不小于约 `3.9rem` 高的触控区，并保持足够间距，避免拇指误触。
- 按钮文字随触控区同步放大；在常见 320～430 CSS 像素宽的竖屏手机上仍不得截断。
- 整组控件继续避开底部安全区，并保持在主角下方或两侧，不能遮挡主角和主要 HUD。

### 46.3 验收

1. Android/iOS 原生壳中，左侧只有“换刃”，右侧同时包含“加速”“减速”和“跳跃”。
2. 原生壳画面中央没有游戏操作按钮，中央空白区域不截获触摸。
3. 四个按钮比上一版触控区更大，单手横跨屏幕时无需触及中央区域。
4. 网页版与 PWA 的 DOM、触控布局、构建产物和公开地址保持不变。

## 47. 雪痕连续性、原生按键顺序与音乐改版

### 47.1 雪板与雪痕接触点

网页版、PWA 和原生手游共用同一套雪痕逻辑，因此本节的雪痕修复同时作用于三种交付形态。

- 雪板属于地面接触物。加速下蹲或减速姿态只能改变人物身体相对雪板的位置，不能把整块雪板沿屏幕纵向推离原接触点。
- 雪痕采样继续以雪板底部为起点，并按世界距离等距插值。
- 从巡航切换到加速、从加速松开回到巡航、以及加速与减速之间切换时，雪痕不得新开段或留下空白。
- 真正腾空时仍停止绘制地面雪痕；落地后从新接触点开始新段，不能跨空中轨迹连线。
- 姿态切换可以改变雪板角度、横向位置和雪痕深浅，但不能改变采样距离的连续性。

### 47.2 原生手机版右侧纵向顺序

本项只修改 `body.native-mobile`，网页版和 PWA 的触控布局保持不变。

- 右侧不再并排显示加速与减速，改为单列纵向操作组。
- 从上到下固定为“减速”“加速”“跳跃”。
- 三个按钮右边缘对齐；“跳跃”继续使用圆形大触控区。
- 左侧仍只有“换刃”，中间仍为空白且不截获触摸。

### 47.3 背景音乐第二版

替换原有高频、密集的程序化电子旋律，继续使用原创 Web Audio，不引入外部版权音乐：

- 风格改为开阔、克制的冬日氛围电子，速度约 92 BPM。
- 使用柔和长音和弦、低频正弦贝斯、稀疏的短旋律和轻量低频节拍。
- 禁止连续密集的高频琶音、明显的方波游戏机音色或尖锐“滴滴”声。
- 主音乐峰值应低于事件音效，循环衔接平滑；开始、暂停、失焦、关闭音乐和卸载时继续遵循第 30.2 节生命周期规则。
- 网页版和原生手游使用同一音乐调度器，音乐开关与既有本地存档兼容。

### 47.4 验收

1. 巡航进入加速以及松开加速后，雪板后方两道雪痕连续，无可见断口。
2. 跳跃期间不画地面雪痕，落地后正常另起连续段。
3. 原生手机版右侧按键从上到下严格为减速、加速、跳跃；网页版布局不变。
4. 新音乐不再使用旧版密集三角波琶音与方波节拍，启停、循环和音量控制无回退。

## 48. 原生隐形速度热区、动作按钮精修与音乐响度

### 48.1 原生加速/减速隐形热区

本项只覆盖 Capacitor 原生手游壳（`body.native-mobile`），网页版和 PWA 的可见触控按钮保持不变。

- 右侧从上到下仍固定为“减速”“加速”“跳跃”。
- “减速”和“加速”不再绘制长条按钮、边框、底色、阴影或文字，改为完全透明的拇指热区。
- 两个透明热区仍保留足够的触控高度、右侧安全区间距和完整的按住/释放语义；按下、移出、取消或失去指针捕获后不能残留输入状态。
- 透明热区只占据屏幕右侧窄列，不扩展到中央游戏画面；中央区域继续不截获触摸。
- 为辅助技术保留明确的“减速”“加速”无障碍名称，视觉隐藏不能删除操作语义。

### 48.2 “换刃”与“跳跃”按钮视觉

- 两个圆形动作按钮使用克制的半透明磨砂玻璃效果、细内外环和柔和色彩光晕，避免厚重描边与粗糙实心底色。
- “换刃”使用青绿色调与方向符号，“跳跃”使用暖橙色调与向上符号；文字层级清晰，不在圆内堆叠冗长说明。
- 保持约 `5.4rem` 的可点击面积、圆形轮廓和安全区避让；视觉装饰不得改变现有操作事件。
- 按下反馈使用轻微缩放、亮度和光晕变化，不产生明显位移，也不能遮挡更多游戏画面。

### 48.3 背景音乐响度

- 在不改变第 47.3 节曲风、速度和编曲的前提下，将主音乐总线由上一版明显提高，目标约增加 7 dB，使手机和笔记本的常用系统音量下都能清楚听见。
- 使用更快的压缩器起音、更高压缩比和适中的软拐点控制多个声部叠加峰值，避免削波、爆音和明显泵动。
- 事件音效继续独立输出；音乐开始淡入、暂停淡出、失焦与卸载清理逻辑保持不变。

### 48.4 验收

1. 原生手机版中看不到“减速”“加速”长条按钮，但按住右侧对应的上、下热区仍分别减速和加速。
2. 透明热区右侧对齐且不进入中央区域；最下方仍为可见的“跳跃”圆形按钮。
3. “换刃”和“跳跃”按钮为精细的半透明圆形控制，文字不重叠，按下反馈稳定。
4. 网页版/PWA 的触控按钮外观和排列不受本节影响。
5. 背景音乐相较上一版明显增大且无可闻削波；音乐生命周期和开关状态无回退。

## 49. 手机应用图标与挖挖机金丝熊改版

### 49.1 手机应用图标

- Android 与 iOS 应用图标从现有《薯薯雪线》宣传海报提炼，不直接把横版海报整体压缩进小图标。
- 图标采用方形构图，以红发主角、雪镜、红色雪板和蓝白雪山为核心识别元素；移除大段标题、英文副标题和过密的远景角色。
- 主体必须落在中央安全区，Android 自适应图标裁切为圆形、圆角方形或水滴形时不能切掉主角脸部和雪板关键轮廓。
- Android 提供各密度普通/圆形图标和自适应前景图；iOS 提供 1024 × 1024 App Store 图标。
- 网页 PWA 图标同步使用同一视觉母版，保持网页安装与原生安装的品牌一致性。

### 49.2 挖挖机金丝熊形象

- 挖挖机从普通橘色仓鼠改为毛茸茸的金丝熊（长毛叙利亚仓鼠）形象。
- 核心特征：金棕色蓬松长毛、奶油白脸颊和腹部、小圆耳、黑色高光眼、粉色鼻子、短小前爪，以及明显的柔软毛边轮廓。
- 宣传/商城图片使用与现有海报一致的明快卡通插画风，采用斜向下奔跑的三分之四视角；不能画成短毛老鼠、豚鼠或普通圆球仓鼠。
- 商城描述改为“毛茸茸金丝熊”，同时保留“跟随时本局薯薯币 +25%”能力。
- 游戏内 Canvas 形象使用分层毛簇、蓬松身体轮廓和奶油色面部区域提高小尺寸识别度；动作和跟随算法保持不变。

### 49.3 宠物身后痕迹

- 上一版宠物痕迹由独立脚掌与脚趾圆点组成，缩放到手机屏幕后会退化成一串不明点状物，因此停止使用离散圆点脚印。
- 改为沿宠物真实跟随轨迹绘制短距离、低对比度的连续浅雪划痕，使用圆角线端和渐淡透明度。
- 两只宠物的划痕必须位于宠物身后，不穿过身体、不突然向更早位置回退，也不与雪板主雪痕混淆。

### 49.4 验收

1. Android/iOS 主屏幕图标能清楚识别《薯薯雪线》主角，常见圆形裁切不丢失主体。
2. 商城图片、文字描述和游戏内挖挖机都明确呈现为毛茸茸的金丝熊。
3. 挖挖机的 +25% 薯薯币能力、购买状态和跟随状态保持兼容。
4. 手机版宠物身后不再出现连续小圆点，改为柔和、连续且短促的雪面划痕。

## 50. 车车金渐层统一卡通形象

### 50.1 双宠物统一视觉语言

- 车车改为与第 49.2 节金丝熊挖挖机相同完成度的卡通插画风，不再在商城中使用简化 CSS 拼接造型。
- 两只宠物图片统一使用明快蓝白雪坡、斜向下三分之四奔跑视角、暖棕细描边、柔和赛璐璐明暗和清晰的小尺寸面部高光。
- 图片构图、主体占比、雪面动势和圆角预览框保持一致，使宠物商城卡片像同一套角色资产。

### 50.2 车车造型

- 车车为毛茸茸的金渐层幼猫：圆润头部、短口鼻、奶油白口鼻与胸口、暖金色渐层被毛、额头和四肢的浅棕虎斑、绿色高光大眼、粉色鼻子和蓬松上翘尾巴。
- 身体保持猫科比例，必须有明显三角耳、四肢和长尾，不能画成仓鼠式圆球、橘色普通短毛猫或写实成年猫。
- 商城描述改为“毛茸茸金渐层幼猫”，继续明确“跟随时开局获得一次护盾”。
- 游戏内 Canvas 版本增加蓬松轮廓、分层金色毛发、奶油胸口和更清楚的虎斑，同时保持现有尺寸、跟随路线与动作节奏。

### 50.3 宣传海报

- 宣传海报中的车车同步替换为统一后的金渐层卡通形象。
- 仅替换金渐层猫，不改变主角、金丝熊挖挖机、标题文字、雪板、雪山背景、构图和整体配色。

### 50.4 功能兼容与验收

1. 商城中的车车和挖挖机采用同样的方形雪坡插画预览，不再一张是图片、一张是 CSS 简图。
2. 游戏内车车能辨认出绿色眼睛、金渐层、奶油口鼻胸口、虎斑和蓬松上翘尾巴。
3. 车车的一次性透明护盾、购买状态、携带状态和旧存档 ID `pet-car` 保持不变。
4. 海报中的车车与商城图片一致，且中文标题“薯薯雪线”和英文“SHUSHU SNOWLINE”保持正确。

## 51. 宠物开局跟随与雪痕稳定性

### 51.1 开局位置

- 开始界面、倒计时和刚进入滑行时，两只宠物必须直接位于各自最终跟随槽位，不先贴近主人公，也不在积累到足够历史距离后突然横跳到另一侧。
- 尚无真实滑行历史时，宠物使用以本局起点为锚点的直线预备轨迹；预备轨迹的侧向偏移方向必须与真实轨迹插值使用的法线方向一致。
- 第一批真实轨迹样本产生后，从预备位置连续过渡到真实跟随位置，不允许因为侧向偏移符号相反而跨越主人公。

### 51.2 宠物雪痕

- 宠物雪痕只能使用宠物已经实际经过的跟随历史，不得用尚未发生的预备轨迹绘制长直线。
- 当历史样本不足两个时不绘制宠物雪痕；积累出至少两个连续有效点后才绘制短促的双道浅痕。
- 雪痕不得连接“预备位置”和“真实历史位置”，不得穿过宠物或主人公，也不得在开局突然从宠物身体后方向远处伸长。

### 51.3 跨平台验收

1. 开始界面与倒计时阶段，挖挖机和车车均稳定待在主人公身后，没有长线或位置跳变。
2. 开始滑行后的前几秒，宠物平滑进入历史跟随；改变方向时不跨侧、不回退。
3. 宠物雪痕从真实运动后逐渐出现，长度短、透明度低，且不会与主人公雪板痕迹连成一条线。
4. Safari 网页版、Android 安装版和 iOS 安装版共用同一跟随修复逻辑。

## 52. iOS Safari 与原生手机版体验统一

### 52.1 手机网页舞台

- iPhone Safari 与 Android/iOS 原生安装版使用同样的全屏舞台策略：占满当前可见动态视口，在刘海、灵动岛和底部手势区之间取最大可用尺寸。
- Canvas 内部继续以 9:16 逻辑尺寸运行；在细长手机屏幕上使用居中 `cover` 缩放，仅裁去左右两侧不参与碰撞的安全雪地区域，不拉伸角色，也不裁切 HUD、商城和触控按钮。
- 手机网页不保留桌面版外围留白、边框和圆角；隐藏桌面品牌栏、侧边说明与键盘提示，避免缩小实际游戏画面。
- 使用 `viewport-fit=cover`、`100dvh` 和四边 `safe-area-inset-*` 处理 Safari 地址栏伸缩及设备安全区。
- Safari 浏览器自身的地址栏仍由系统控制；添加到主屏幕后应呈现最接近原生安装版的独立应用体验。

### 52.2 触控操作统一

- 手机 Safari、手机 Chrome、Android 安装版和 iOS 安装版统一为：左侧仅显示“换刃”，右侧从上到下依次为“减速”隐形热区、“加速”隐形热区和“跳跃”圆形按钮。
- 中间区域不得放置触控按钮或透明热区，必须保留完整雪道视野。
- “换刃”和“跳跃”沿用原生手机版的 `5.4rem` 半透明圆形设计、图标、按下反馈和安全区避让。
- 隐形调速区仍保留无障碍名称、按住持续生效、指针捕获与松手复位逻辑。

### 52.3 桌面与跨平台回归

- 桌面宽屏网页版继续显示原有品牌栏、键盘说明和桌面布局，不因手机网页统一而改变。
- 商城、音乐、宠物、服装、雪板、存档与游戏数值继续共用同一套业务逻辑；平台差异只允许出现在系统能力层，例如原生触感反馈。
- 后续每个游戏问题默认同时排查桌面网页、手机 Safari/Chrome、Android 安装版与 iOS 安装版；若问题仅存在于单一平台，必须在验收结果中明确说明。

### 52.4 验收

1. iPhone Safari 竖屏打开后，游戏舞台使用全部可见高度，不再以 9:16 卡片缩在页面中央或留下明显上下黑边。
2. Safari 与原生手机版的触控区域位置、尺寸和顺序一致，中间雪道无按钮遮挡。
3. 刘海、灵动岛、Safari 工具栏和底部手势区不会遮挡 HUD、商城关闭键或触控按钮。
4. 桌面浏览器宽屏布局、键盘操作和左右品牌说明保持不变。

## 53. 跳跃宠物、刹车姿态、雪裤辨识度与刻滑预研

### 53.1 跳跃与峡谷期间的宠物跟随

- 网页版、手机浏览器、Android 与 iOS 继续共用同一套宠物轨迹逻辑。
- 角色腾空时雪板不会生成新的地面轨迹；当宠物目标距离超过最后一个有效轨迹样本时，宠物必须稳定使用“最新有效轨迹段”的方向和侧向槽位，不能回退到最早轨迹点或本局起点。
- 腾空期间宠物的纵向滞后距离保持在各自槽位附近，横向位置不得突然跳到雪道边界；落地生成新轨迹后再连续过渡到新的真实历史位置。
- 超出真实历史范围的临时位置不得用于绘制宠物雪痕，避免跨越峡谷或连接到边界的长线。
- 挖挖机与车车均执行相同规则；高速、连续换刃和跳过峡谷组合场景下也不能飞出画面后再返回。

### 53.2 L 减速时的横板刹雪姿态

- 按住 `L` 或手机“减速”热区时，雪板相对当前巡航雪板方向旋转严格约 90°，表现为横板推雪减速，而不是只增加一个小角度。
- 雪板旋转与人物上半身旋转分离：雪板完成横板动作，人物保持可读的站立/屈膝姿态，只允许适度侧身，不能随雪板整体横躺。
- 双脚、固定器与腿部落点跟随横板后的固定器位置，避免脚与雪板脱离。
- 雪板底部接触点、雪痕采样点和刹雪飞雪方向使用横板后的实际角度，按下与松开 `L` 时仍保持雪痕连续。

### 53.3 主角腿部与雪裤辨识度

- 双腿从细线式造型改为有明显宽度的雪裤轮廓，包含臀胯、膝部和裤脚层次；常态、加速下蹲与横板刹雪姿态都不能退化为两根细线。
- 裤腿彩色主体宽度相较上一版明显增加，并增加深色外轮廓，使雪裤与雪板、雪地和上衣分离。
- 棋盘、工装口袋、火焰、极光等雪裤装饰同步放大并跟随膝部/裤腿位置，在手机缩放后的 Canvas 中仍能分辨装备变化。
- 商城物品 ID、价格、购买/装备状态和旧存档保持不变，本项只增强游戏内穿着后的视觉差异。

### 53.4 刻滑方案独立 Demo

- 当前正式游戏继续保留“落叶飘式左右换刃”路线，本轮不直接修改正式游戏的转弯模型、碰撞、速度或摄像机。
- 单独制作一个不位于 `web/`、`mobile/` 正式源码目录内的交互 Demo，用于对比“当前落叶飘”和“拟议刻滑”。
- 刻滑方案展示连续 S 形圆弧、换刃过渡、人物随切线方向适度侧身，以及板刃压雪产生的弧形轨迹；人物不必始终完全正对玩家。
- Demo 只承担视觉与运动方向评估，不写入正式存档、不进入 Sites 生产构建；待用户确认后再另行设计正式玩法参数。

### 53.5 背景音乐第三次增益

- 保持第 47.3 节的冬日氛围电子曲风、92 BPM、旋律与节奏结构不变，只调整共享 Web Audio 输出级。
- 相对第 48.3 节当前版本再提高约 6～8 dB；手机、Safari、安卓原生壳和笔记本在常用系统音量下应能清楚听见和弦、低音与节拍。
- 主音乐总线提高到约 `0.84`，压缩器调整为较温和的阈值与压缩比，并增加约 `1.18` 的输出补偿；多个声部重叠时保留足够余量，不得出现削波、爆音或明显泵动。
- 音乐开关、首次用户手势解锁、暂停/失焦淡出、恢复播放与卸载清理逻辑保持不变。

### 53.6 验收

1. 高速跳过峡谷时，两只宠物保持在主人公后方合理槽位，不冲到左右或上下画面边界。
2. 落地后宠物平滑接回新轨迹，宠物雪痕不跨峡谷、不出现长直线。
3. 按住 `L` 时雪板相对巡航方向呈约 90°横板刹雪，人物没有整体横躺，脚仍连接固定器。
4. 任意雪裤装备在常态、加速与减速姿态下都比上一版更粗实，主要图案在手机尺寸可辨认。
5. 桌面网页、手机 Safari/Chrome、Android 和 iOS 共用前三项修复；独立刻滑 Demo 不改变正式游戏源文件和线上玩法。
6. 新版背景音乐相对当前线上版本明显增大，四个平台使用同一增益与压缩设置，且没有可闻失真。

## 54. 刹车人物整体横转与高速摩擦火星

### 54.1 覆盖关系

- 本节覆盖第 53.2 节“雪板与人物上半身分离旋转”以及第 53.6 节第 3 条“人物不整体横转”的旧要求。
- 刻滑预研仍保持为独立 Demo，本节只修改已经上线的 `L` 减速/手机减速热区，不把刻滑路线写入正式游戏。

### 54.2 人物与雪板统一刹车方向

- 按住 `L` 或手机减速热区且人物位于地面时，人物、双腿、固定器和雪板作为同一个整体，相对巡航状态统一旋转约 90°。
- 人物不能继续保持原来的正向姿态；身体方向必须与横板刹雪方向一致，松开减速后整体恢复巡航方向。
- 双脚与固定器在整个刹车姿态中保持连接，不允许雪板已经横转而人物仍停留在原角度。
- 雪板接触点、轨迹采样、喷雪与碰撞仍使用整体横转后的实际方向，不能破坏加速、减速切换时的雪痕连续性。

### 54.3 高速摩擦火星

- 人物在地面执行横板刹雪时，雪板下沿/接触雪面的区域产生短促的橙黄色摩擦火星，表现高速刹车的强调特效。
- 火星数量、长度和亮度随当前速度提高而增强；低速刹车仅有少量火星，高速刹车更明显。
- 火星必须是短线与小亮点的瞬时粒子，沿雪板接触边缘向后甩出，不得画成持续光束、喷气尾焰或雪板下方两条蓝色图案。
- 腾空、撞车、普通滑行和加速状态不显示火星；特效只影响画面，不修改速度、碰撞、护盾或薯薯币逻辑。

### 54.4 跨端验收

1. 桌面网页按住 `L`、手机网页与 Android/iOS 按住减速热区时，人物和雪板均相对巡航方向统一横转约 90°。
2. 任一平台都不存在“雪板已经垂直、人物仍保持原方向”的脱节姿态，脚和固定器保持连接。
3. 地面高速刹车能看到雪板下沿的橙黄色短促火星，低速时减弱，腾空或非刹车状态完全消失。
4. 网页版与手机版继续使用 `web/app/game/` 同一实现，不新增分叉的移动端游戏逻辑。
